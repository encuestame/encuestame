define([ "dojo/_base/declare", "me/core/enme" ], function(declare, _ENME) {

  return declare(null, {

    /*
     *
     */
    constructor : function() {
    },

    /**
     * Enables the block items when tweetpoll is processing.
     */
    enableBlockTweetPollOnProcess : function() {
      //deprecated // should be removed
    }

  });
});
