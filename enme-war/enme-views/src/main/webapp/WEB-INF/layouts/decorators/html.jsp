<!DOCTYPE html>
<!--[if IE 9]>     <html data-so="${so}" data-bo="${agent_family}" data-vo="${bo_vendor_ver}" data-bo-name="${agent_name}" class="ie ie9 lte9 ${agent_family}"> <![endif]-->
<!--[if gt IE 9]>  <html data-so="${so}" data-bo="${agent_family}" data-vo="${bo_vendor_ver}" data-bo-name="${agent_name}" class="ie9 ${agent_family}"> <![endif]-->
<!--[if !IE]><!-->
<html data-so="${so}" data-bo="${agent_family}" data-vo="${bo_vendor_ver}" data-bo-name="${agent_name}" lang="${user_locale}" class="${agent_family}">
<!--<![endif]-->