<!DOCTYPE html>
<!--[if IE 9]>     <html data-so="${so}" data-bo="${agent_family}" data-vo="${bo_vendor_ver}" data-bo-name="${agent_name}" class="ie ie9 lte9 ${so}"> <![endif]-->
<!--[if gt IE 9]>  <html data-so="${so}" data-bo="${agent_family}" data-vo="${bo_vendor_ver}" data-bo-name="${agent_name}" class="ie9 ${so}"> <![endif]-->
<!--[if !IE]><!-->
<html data-so="${so}" data-bo="${agent_family}" data-vo="${bo_vendor_ver}" data-bo-name="${agent_name}" lang="${user_locale}" class="${so}">
<!--<![endif]-->